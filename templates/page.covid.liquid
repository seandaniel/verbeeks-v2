<div class="wrapper">
  <h1 class="secondary-h1">{{ page.title }}</h1>
  <div class="covid-text-img-container">
    {{ page.content }}
    <div class="svg-container">
      <img src="{{ 'mask.svg' | asset_url }}" alt="A person wearing a mask">
    </div>
  </div>
</div>